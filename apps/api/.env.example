BASE_URL="http://localhost:3333"
BETTER_AUTH_URL="http://localhost:3000"
NEXT_PUBLIC_APP_URL="http://localhost:3000"
NEXT_PUBLIC_PORTAL_URL="http://localhost:3002"
AUTH_COOKIE_DOMAIN= # Optional, e.g. .example.com for cross-subdomain cookies
AUTH_TRUSTED_ORIGINS=http://localhost:3000,http://localhost:3002,http://localhost:3333
AUTH_ENFORCED_PROVIDER= # Optional: google | github | microsoft. Set to microsoft for Entra-only access
INTERNAL_TEAM_EMAIL_DOMAINS= # Optional CSV, e.g. example.com,subsidiary.example.com
SECRET_KEY="local-dev-secret-key-at-least-16-chars"
NODE_EXTRA_CA_CERTS=/etc/ssl/cert.pem
PORT="3333"

APP_AWS_BUCKET_NAME=compai-local-dev
APP_AWS_QUESTIONNAIRE_UPLOAD_BUCKET=compai-local-dev-questionnaires
APP_AWS_KNOWLEDGE_BASE_BUCKET=compai-local-dev-knowledge-base
APP_AWS_REGION=us-east-1
APP_AWS_ACCESS_KEY_ID=local-dev-access-key
APP_AWS_SECRET_ACCESS_KEY=local-dev-secret-key
APP_AWS_ORG_ASSETS_BUCKET=compai-local-dev-org-assets
APP_AWS_ENDPOINT="" # optional for using services like MinIO

# Microsoft sign-in (Entra ID / Azure AD)
AUTH_MICROSOFT_CLIENT_ID=
AUTH_MICROSOFT_CLIENT_SECRET=
AUTH_MICROSOFT_TENANT_ID= # Required when AUTH_ENFORCED_PROVIDER=microsoft; use your tenant GUID, not common

DATABASE_URL="postgresql://postgres:postgres@localhost:5432/comp"

NOVU_API_KEY=

# Service tokens for internal service-to-service auth (scoped, per-service)
SERVICE_TOKEN_TRIGGER= # Used by Trigger.dev tasks
SERVICE_TOKEN_PORTAL=  # Used by Portal app

# Upstash
UPSTASH_REDIS_REST_URL=
UPSTASH_REDIS_REST_TOKEN=

UPSTASH_VECTOR_REST_URL=
UPSTASH_VECTOR_REST_TOKEN=

# Trigger
TRIGGER_SECRET_KEY=

OPENAI_API_KEY=
ANTHROPIC_API_KEY=
GROQ_API_KEY=

# Resend (for sending emails)
RESEND_API_KEY=
RESEND_FROM_SYSTEM= # e.g., noreply@mail.trycomp.ai
RESEND_FROM_DEFAULT= # e.g., hello@mail.trycomp.ai
RESEND_FROM_TRUST_PORTAL= # e.g., Comp AI <noreply@mail.trust.inc> — sender for Trust Portal access/NDA emails (falls back to RESEND_FROM_SYSTEM)

# Background checks
BACKGROUND_CHECK_API_BASE_URL=https://glad-sturgeon-729.convex.site
BACKGROUND_CHECK_API_KEY=
MACED_API_KEY=mc_dev_dummy_api_key
BACKGROUND_CHECK_WEBHOOK_SECRET=
BACKGROUND_WH_ENDPOINT=
STRIPE_BACKGROUND_CHECK_PRICE_ID=price_1TRWckCkFWhKYvHIA1GLv1sO

# We are not using security hub service it is only the variable prefix for matching with old one
SECURITY_HUB_ROLE_ASSUMER_ARN=
SECURITY_HUB_GOVCLOUD_ROLE_ASSUMER_ARN=
SECURITY_HUB_GOVCLOUD_ACCESS_KEY_ID=
SECURITY_HUB_GOVCLOUD_SECRET_ACCESS_KEY=
# Optional: only set when using temporary GovCloud credentials. Leave unset for long-lived IAM user keys.
# SECURITY_HUB_GOVCLOUD_SESSION_TOKEN=
